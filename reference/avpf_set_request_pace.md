# Set the Alpha Vantage max requests per minute

`avpf_set_request_pace()` sets Alphavantage API key and entitlement code

## Usage

``` r
avpf_set_request_pace(max_requests_per_min = 60)
```

## Arguments

- max_requests_per_min:

  (default 60) Maximum requests per minute

## Value

Nothing

## Details

Change this if you have a better plan with AlphaVantage

## See also

[`avpf_api_key()`](https://derekholmes0.github.io/alphavantagepf/reference/avpf_api_key.md)
