# Extract data from Alpha Vantage retuned data

Extract data from Alpha Vantage retuned data

## Usage

``` r
av_make_funcmap()
```

## Value

function map dat.table, also written to data directory

## Details

Creates a data.table with API signatures and default values for
Alphavantage API calls

## See also

[`av_get_pf()`](https://derekholmes0.github.io/alphavantagepf/reference/av_get_pf.md)

## Examples

``` r
if (FALSE) { # \dontrun{
av_make_funcmap()
} # }
```
